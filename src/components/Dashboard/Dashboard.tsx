import Card from "../Card";

type DashboardProps = {};

const Dashboard: React.FC<DashboardProps> = ({}) => {
  return (
    <div>
      <Card />
      <Card />
      <Card />
    </div>
  );
};

export default Dashboard;
